# C22 · Retry storm

Wave 5 — failure experiments (results only)

Each accepts when its `results/NNN-*.md` exists with the template filled and a ≤5-line conclusion.

Owner: `results/` (derived) · Needs: C18 (derived) — PLAN.md states neither

Build: Retry storm: retries on, sim slow, no breaker → outage; breaker on → recovery

Accept when:
- [ ] `results/NNN-*.md` exists with the template filled and a ≤5-line conclusion
